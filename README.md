# Management Updates

## Setup

```bash
npm i -g @modelcontextprotocol/server-slack airtable-mcp-server
cp mcp/slack/index.js $NODE_HOME/node_modules/@modelcontextprotocol/server-slack/dist/index.js
```

Copy `.mcp.example.json` to `.mcp.json` and add your channel name and tokens.
